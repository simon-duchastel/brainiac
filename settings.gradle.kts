rootProject.name = "brainiac"

include(
    "core",
    "core:identity",
    "core:fileaccess",
    "core:llm",
    "core:process",
    "core:process:core-loop",
    "core:process:search",
    "agents",
)