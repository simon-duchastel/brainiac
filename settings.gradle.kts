rootProject.name = "brainiac"

include(
    "core",
    "core:identity",
    "core:fileaccess",
    "core:search",
    "core:process",
    "core:process:core-loop",
    "core:process:search",
    "agents",
)